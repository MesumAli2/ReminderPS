<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Reminders" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:actionOverflowButtonStyle"> @style/MyThemeUpper</item>
        <item name="menuItemColor">#ffffff</item>
        <item name="addFragColor">@color/black</item>
        <item name="reminderItem">@color/dark</item>
        <item name="addEditTask">@color/dark</item>
        <item name="welBtn">@color/teal_200</item>
        <item name="checkBoxColor">@color/white</item>

        <!--System status bar color -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:colorAccent">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/black</item>
        <item name="colorControlHighlight">@color/black</item>


        <!--Displays the text for toolbar  -->
        <!-- Theme for styling the floating button-->
        <!-- Theme for styling the back arrow button . -->

        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="actionMenuTextColor">@color/white</item>

        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/black</item>
        <!-- For Android sdk versions 26+. -->

        <!-- sets the style for the overflow button -->
        <item name="actionOverflowButtonStyle">@style/actionOverflowButtonStyleNight</item>

        <!-- Status bar color. -->
        <item name="android:colorControlActivated">@color/darkgrey</item>

        <item name="actionOverflowMenuStyle">@color/white</item>



        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorPrimary">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>


        <!-- Theme for styling the floating button and selected dates in calendar . -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:textColor"> @color/white</item>
        <!-- For Android sdk versions 26+. -->
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@font/opensans_font</item>
        <!-- Target Android sdk versions < 26 and > 14. -->
        <item name="fontFamily">@font/opensans_font</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/statusbardark</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--Theme for Date Time picker and Calendar time picker buttons  -->
        <item name="android:buttonBarPositiveButtonStyle">@style/AppTheme.Alert.Button.Positive</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/AppTheme.Alert.Button.Negative</item>
        <item name="android:buttonBarNeutralButtonStyle">@style/AppTheme.Alert.Button.Neutral</item>

        <!--CheckBox border color -->
        <!-- Customize your theme here. -->
    </style>

    <style name ="TextAppearance.AppCompat.Widget.ActionBar.Title." parent="@android:style/TextAppearance">
        <item name="android:textSize">5sp</item>
    </style>


    <!--Styles Date Time picker and Calendar time picker buttons  -->
    <style name="AppTheme.Alert.Button.Positive" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/white</item>
    </style>
    <!--Styles Date Time picker and Calendar time picker buttons  -->
    <style name="AppTheme.Alert.Button.Negative" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/white</item>
    </style>



    <style name="splashScreenThemeDark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/cardview_dark_background</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>
    <!--Styles the 3 dot buttons on the edge of the applicaiton -->
    <style name="actionOverflowButtonStyleNight" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/white</item>
    </style>

    <style name="MyThemeUpper" parent="android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_baseline_search_24</item>
    </style>

    <style name="splashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_image_dark</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>
</resources>